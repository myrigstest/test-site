{
  "name": "test-site",
  "version": "1.0.0",
  "description": "Responsive one-page starter site for a wedding photographer, built with HTML5, Tailwind CSS (CDN), and vanilla JavaScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/styles.css --minify",
    "build": "npm run build:css"
  },
  "repository": {
    "type": "git",
    "url": "http://localhost:26831/myrigstest/test-site"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
